{
  "name": "mock-api-server",
  "version": "1.0.0",
  "description": "Mock REST API for testing",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --static ./public --port 5000 --routes routes.json",
    "start:custom": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
